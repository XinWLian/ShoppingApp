<UserControl x:Class="ShoppingApp.View.SelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShoppingApp.View"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="100">
    <Grid>
        <StackPanel Orientation="Vertical">
            <RadioButton Style="{DynamicResource SelectorButton}" 
                    Content="Produce" Command="{Binding SelectProduceCommand}" Width="100" Height="100" />
            <RadioButton Style="{DynamicResource SelectorButton}" 
                    Content="Meat" Command="{Binding SelectMeatCommand}" Width="100" Height="100"/>
            <RadioButton Style="{DynamicResource SelectorButton}" 
                    Content="Frozen" Command="{Binding SelectFrozenCommand}" Width="100" Height="100"/>
            <RadioButton Style="{DynamicResource SelectorButton}" 
                    Content="Beverages" Command="{Binding SelectBeverageCommand}" Width="100" Height="100"/>
        </StackPanel>
    </Grid>
</UserControl>
